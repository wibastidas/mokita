// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyBoOv4hkpbQs3bF1u1ezkGdkS0wendSJEY',
    authDomain: 'mokita-app.firebaseapp.com',
    databaseURL: 'https://mokita-app.firebaseio.com',
    projectId: 'mokita-app',
    storageBucket: 'mokita-app.appspot.com',
    messagingSenderId: '887068973756',
    appId: '1:887068973756:web:0464625d3a4da05e078eb9',
    measurementId: 'G-0P9F92820S'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
